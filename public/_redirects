/*
  This redirect rule is required for Single Page Applications (SPAs) 
  like React to handle client-side routing on Render.
  It tells the server to serve index.html for all requests, 
  letting the React Router handle the URL.
*/
/*    /index.html   200
